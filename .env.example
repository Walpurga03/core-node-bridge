# Beispiel-Konfiguration für Core Node Bridge

# =============================================
# Konfiguration für die Hauptanwendung (z.B. Mainnet)
# Wird aus der .env-Datei gelesen
# =============================================

# Bitcoin Core RPC Username
RPC_USER=dein_rpc_user

# Bitcoin Core RPC Passwort
RPC_PASSWORD=dein_rpc_passwort

# Host/IP deines Bitcoin Core Nodes (z.B. 127.0.0.1 oder eine interne IP)
RPC_HOST=127.0.0.1

# Port deines Bitcoin Core RPC (Standard: 8332)
RPC_PORT=8332

# =============================================
# Konfiguration für die Backend-Tests (regtest)
# Muss in einer separaten .env.test-Datei gespeichert werden
# Siehe TESTING.md für Details
# =============================================

# Beispiel für .env.test:
# RPC_USER=myuser
# RPC_PASSWORD=mypass
# RPC_HOST=127.0.0.1
# RPC_PORT=18443